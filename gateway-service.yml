server:
  port: 8222
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users
        - id: discovery-service
          uri: http://localhost:8761
          predicates:
                - Path=/
